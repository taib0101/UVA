#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,sum=0;
    while(cin>>n)
    {

            if(n%2 ==0)
                sum = n+(n/2);
            else
                sum = n+(n/2);
        cout<<sum<<endl;
        sum = 0;
    }
   return 0;
}
