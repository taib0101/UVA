#include<bits/stdc++.h>
using namespace std;

int main()
{
    int v,t,s;
    while(cin>>v>>t)
    {
        if((-100 <= v&&v <= 100)&&(0 <= t&&t <= 200))
        {
            s = v*t;
            cout<<s+s<<endl;
        }
        else
            break;
    }
    return 0;
}
